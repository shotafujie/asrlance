[project]
name = "asrlance"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "espnet>=202511",
    "espnet-model-zoo>=0.1.7",
    "psutil>=7.2.0",
    "torch>=2.9.1",
    "torchaudio>=2.9.1",
]
